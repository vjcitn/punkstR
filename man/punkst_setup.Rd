% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{punkst_setup}
\alias{punkst_setup}
\title{Configure the punkst executable and Python interpreter}
\usage{
punkst_setup(bin = NULL, python = NULL)
}
\arguments{
\item{bin}{Path to the \code{punkst} binary.}

\item{python}{Path to a Python interpreter that has \code{spatialdata} installed.}
}
\value{
The resolved settings, invisibly, as a list.
}
\description{
Records where to find the \code{punkst} binary and the Python interpreter used
for the SpatialData export script. Values are stored in
\code{options(punkstR.bin, punkstR.python)}. Nothing is run or validated here;
use \code{\link[=check_punkst_setup]{check_punkst_setup()}}.
}
\details{
Resolution order when an argument is \code{NULL}: the option, then the
environment variable (\code{PUNKST}, \code{PUNKST_PYTHON}), then the \code{PATH}.
}
