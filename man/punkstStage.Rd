% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{punkstStage}
\alias{punkstStage}
\alias{punkstTiles}
\alias{punkstHex}
\alias{punkstModel}
\title{Pipeline stage (abstract parent class)}
\usage{
punkstStage(workdir = character(0), files = list(), params = list())

punkstTiles(workdir = character(0), files = list(), params = list())

punkstHex(workdir = character(0), files = list(), params = list())

punkstModel(workdir = character(0), files = list(), params = list())
}
\arguments{
\item{workdir}{Directory holding the stage's outputs.}

\item{files}{Named list of output file paths. Required names: \code{punkstTiles}
\code{tsv}, \code{index}, \code{features}; \code{punkstHex} \code{data}, \code{meta}; \code{punkstModel}
\code{model}, \code{state} (plus \code{results} and \code{pseudobulk} when fitted with
\code{transform = TRUE}).}

\item{params}{Named list of the parameters that produced the outputs. These
also drive the skip-if-up-to-date manifest and are recorded by
\code{\link[=sdata_writeback]{sdata_writeback()}}.}
}
\value{
An object of the respective subclass.
}
\description{
Common structure of the objects returned by \code{\link[=punkst_pts2tiles]{punkst_pts2tiles()}},
\code{\link[=punkst_tiles2hex]{punkst_tiles2hex()}} and \code{\link[=punkst_topic_model]{punkst_topic_model()}}. \code{punkstStage} itself is
abstract; use the subclasses \code{punkstTiles}, \code{punkstHex} and \code{punkstModel}.
}
