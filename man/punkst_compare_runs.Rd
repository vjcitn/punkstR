% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writeback.R
\name{punkst_compare_runs}
\alias{punkst_compare_runs}
\title{Compare two punkst runs}
\usage{
punkst_compare_runs(a, b)
}
\arguments{
\item{a, b}{Runs to compare: \code{punkstRun}, \code{punkstModel}, or model file paths.}
}
\value{
A list with \code{pairs} (data frame: \code{topic_a}, \code{topic_b}, \code{cosine}),
\code{similarity} (full topic x topic cosine matrix), \code{n_shared_genes},
and, when the grids match, \code{agreement} (fraction of shared hexagons whose
top topics are paired) and \code{n_shared_hexagons}.
}
\description{
Topics have no fixed identity between fits, so each topic of \code{a} is paired
with its most similar topic of \code{b} (cosine similarity of the gene loadings,
over genes present in both; greedy one-to-one matching, best pairs first).
When both runs use the same hexagon grid the per-hexagon top-topic
assignments are also compared under that pairing.
}
\details{
Works on files, so it needs no Python: give \code{punkstRun}/\code{punkstModel}
objects or paths to \verb{*.model.tsv}.
}
