% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writeback.R
\name{sdata_writeback}
\alias{sdata_writeback}
\title{Write punkst results back into a SpatialData store}
\usage{
sdata_writeback(
  run,
  hex = NULL,
  sdata = NULL,
  out = NULL,
  in_place = FALSE,
  name = NULL,
  points_key = NULL,
  coordinate_system = "intrinsic",
  overwrite = FALSE,
  python = NULL,
  log = tempfile("writeback", fileext = ".log")
)
}
\arguments{
\item{run}{A \code{punkstRun} from \code{\link[=run_punkst_pipeline]{run_punkst_pipeline()}}, or a \code{punkstModel}
from \code{\link[=punkst_topic_model]{punkst_topic_model()}} (then also give \code{hex} and \code{sdata}).}

\item{hex}{A \code{punkstHex}; taken from \code{run} when that is a \code{punkstRun}.}

\item{sdata}{The original store; taken from \code{run} when possible.}

\item{out}{Path of the sidecar store to create or extend. Default
\verb{<sdata minus .zarr>_punkst.zarr}. Ignored when \code{in_place = TRUE}.}

\item{in_place}{Write into \code{sdata} itself instead of a sidecar store.}

\item{name}{Run name used in element names. Default: the model output stem
(e.g. \code{hex_12.k12}), which already encodes hexagon spacing and topic
count; pass your own (e.g. \code{"k12_alpha0.1"}) when tuning other settings.}

\item{points_key}{Points element whose transformations the hexagons inherit;
taken from \code{run} when possible.}

\item{coordinate_system}{Coordinate system used for the export (see
\code{\link[=sdata_export]{sdata_export()}}); \code{"intrinsic"} (default), a name, or \code{NULL} for the
element's only system; taken from \code{run} when possible.}

\item{overwrite}{Replace an existing run of the same name.}

\item{python}{Interpreter override; see \code{\link[=punkst_setup]{punkst_setup()}}.}

\item{log}{Log file.}
}
\value{
The path of the store written to (invisibly), with attributes
\code{run}, \code{shapes}, \code{table}.
}
\description{
Adds one \emph{run} to a SpatialData store: a \code{shapes} element of circles at the
hexagon centres (\verb{punkst_<run>_hexagons}) and a \code{table} (\verb{punkst_<run>})
holding the hexagon x gene counts, the per-hexagon topic probabilities
(\code{obsm["topics"]}), the gene x topic loadings (\code{varm["loadings"]}) and the
parameters that produced them (\code{uns["punkst"]}). The table is linked to the
shapes through \code{region}/\code{instance_id}.
}
\details{
Because element names carry the run name, any number of runs -- different
topic counts, hexagon sizes, seeds, priors -- can coexist in one store and be
compared with \code{\link[=sdata_runs]{sdata_runs()}} and \code{\link[=punkst_compare_runs]{punkst_compare_runs()}}. Writing a run name
that already exists is an error unless \code{overwrite = TRUE}.

By default results go to a \strong{new sidecar store} (\code{out}), which holds only
the punkst elements and is read alongside the original. With
\code{in_place = TRUE} they are added to the store the analysis started from
(this modifies it; the original elements are left untouched).

The hexagon shapes inherit the transformations of the source Points element
when the export used \code{coordinate_system = "intrinsic"} (so, for Xenium, the
micron-to-pixel scale is preserved and the hexagons overlay the images and
transcripts). If the export used a named coordinate system the shapes are
registered in that system with an identity transformation.
}
