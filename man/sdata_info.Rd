% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{sdata_info}
\alias{sdata_info}
\title{Summarise a SpatialData store}
\usage{
sdata_info(
  sdata,
  points_key = "transcripts",
  python = NULL,
  log = tempfile("sdata_info", fileext = ".log")
)
}
\arguments{
\item{sdata}{Path to the SpatialData \code{.zarr} store.}

\item{points_key}{Points element for which row count and coordinate extent
are computed (this can take a while for large stores). \code{NULL} computes it
for every points element.}

\item{python}{Interpreter override; see \code{\link[=punkst_setup]{punkst_setup()}}.}

\item{log}{Log file for the script's output.}
}
\value{
A list with \code{points} (per element: \code{columns}, \code{attrs}, \code{rows},
\code{max_x}, \code{max_y}, \code{transformations}), \code{images} (pixel sizes at full
resolution), \code{shapes}, \code{tables} and \code{spatialdata_version}.
}
\description{
Runs the bundled \code{sdata_info.py} with \code{system2()} and reads the JSON it
writes. Useful for checking which coordinate system holds microns before
exporting.
}
