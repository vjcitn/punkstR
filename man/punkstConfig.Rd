% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{punkstConfig}
\alias{punkstConfig}
\title{Resolved punkst configuration}
\usage{
punkstConfig(
  bin = character(0),
  python = character(0),
  bin_ok = logical(0),
  python_ok = logical(0),
  spatialdata_version = character(0),
  zarr_version = character(0),
  notes = character(0)
)
}
\arguments{
\item{bin}{Path to the punkst executable, or \code{NA_character_} if none found.}

\item{python}{Path to the Python interpreter, or \code{NA_character_}.}

\item{bin_ok}{\code{TRUE} if \code{bin} exists, is executable and prints punkst's
help text.}

\item{python_ok}{\code{TRUE} if \code{python} can import \code{spatialdata} and \code{zarr}.}

\item{spatialdata_version, zarr_version}{Versions reported by the
interpreter, \code{NA_character_} if unavailable.}

\item{notes}{Character vector of actionable messages about what is missing
(never installs anything; points to the build recipe).}
}
\value{
A \code{punkstConfig} object; use \code{@} to read its properties.
}
\description{
Holds the punkst executable and Python interpreter that were resolved
(argument, then option, then environment variable, then \code{PATH}; see
\code{\link[=punkst_setup]{punkst_setup()}}) together with what checking them found. It is created by
\code{\link[=check_punkst_setup]{check_punkst_setup()}}; you would rarely construct one yourself. A
\code{punkstConfig} can be passed wherever a \code{bin} or \code{python} argument is
accepted, in which case the corresponding path is used.
}
