% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{sdata_export}
\alias{sdata_export}
\title{Export a SpatialData points element to punkst transcript format}
\usage{
sdata_export(
  sdata,
  out,
  points_key = "transcripts",
  coordinate_system = "intrinsic",
  min_qv = NULL,
  qv_column = "qv",
  feature_column = NULL,
  overwrite = FALSE,
  python = NULL,
  log = paste0(out, ".export.log")
)
}
\arguments{
\item{sdata}{Path to the SpatialData \code{.zarr} store.}

\item{out}{Output path (\code{.tsv} or \code{.tsv.gz}).}

\item{points_key}{Name of the points element.}

\item{coordinate_system}{Coordinate system to export in, or \code{"intrinsic"}
for the stored coordinates. \code{NULL} lets the script infer it when only one
is registered.}

\item{min_qv}{Drop rows with quality below this value (e.g. 20 for Xenium).}

\item{qv_column}{Name of the quality column.}

\item{feature_column}{Feature column; default is the element's feature key.}

\item{overwrite}{Re-export if \code{out} already exists.}

\item{python}{Interpreter override; see \code{\link[=punkst_setup]{punkst_setup()}}.}

\item{log}{Log file for the script's output.}
}
\value{
The output path, invisibly.
}
\description{
Runs the bundled \code{spatialdata_to_punkst.py} with \code{system2()}; no Python
module is loaded into R. The output is a tab-separated file with a
\verb{#}-prefixed header and columns \code{x}, \code{y}, \code{feature}, ready for
\verb{punkst pts2tiles}.
}
\details{
For Xenium stores written by \code{spatialdata-io}, the stored coordinates are
microns while the registered \code{global} coordinate system is image pixels, so
the default \code{coordinate_system = "intrinsic"} exports microns as punkst
expects.
}
